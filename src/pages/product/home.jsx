import React, { Component } from 'react';

// product的默认子路由页面
export default class ProductHome extends Component {
    render() {
        return (
            <div>
                ProductHome
            </div>
        );
    }
}